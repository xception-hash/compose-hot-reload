name: e2e

# Runs the on-device hot-reload gate (e2e/run.sh, 16 cases) on an Ubuntu KVM
# runner with an API 36 x86_64 AVD, matching the pinned local emulator. The host
# JDK is Temurin 21 (CI has no JBR); this is fine because all JVMTI/dex work runs
# on-device (ART), the host JVM only orchestrates the watch loop.

on:
  # Skip the ~13-min emulator run for docs-only changes. paths-ignore fails safe:
  # a commit that touches ANY non-doc path still runs. The workflow file itself is
  # not ignored, so changes to it are always validated.
  push:
    branches: [main]
    paths-ignore: &doc-paths
      - '**/*.md'
      - 'docs/**'
      - 'tasks/**'
      - 'LICENSE'
  pull_request:
    branches: [main]
    paths-ignore: *doc-paths

# Cancel superseded runs on the same ref (emulator jobs are slow).
concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

# Least privilege (T32 item 4): the job only checks out and uploads a log artifact on
# failure. upload-artifact needs no extra scope beyond the default token, so keep the top
# level read-only rather than the broader default GITHUB_TOKEN permissions.
permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      # Hardware acceleration for the x86_64 emulator.
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # Temurin 21 — CI has no JBR; JBR-specific behavior is not required (see header).
      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '21'

      # SDK components the harness needs:
      #  - build-tools;36.0.0 : the CLI resolves d8 at $ANDROID_HOME/build-tools/36.0.0/d8
      #  - ndk;28.2.13676358  : runtime-client builds the native agent .so (x86_64 in abiFilters)
      #  - cmake;3.22.1        : AGP-bundled default for the CMake build (min required 3.22)
      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3.2.2
      - name: Install pinned SDK components
        run: |
          sdkmanager --install \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "ndk;28.2.13676358" \
            "cmake;3.22.1"

      - uses: gradle/actions/setup-gradle@da187c8e6ffbd3802e00f2477aa5a822b25f2dda # v4.4.4

      # scripts/icon-pixel.sh reads a screencap centre-pixel via Pillow for the
      # drawable/bitmap cases (11-13) — every other case uses uiautomator text
      # dumps. Pillow is the suite's only host-side Python dep and isn't in the
      # runner image; install via apt to sidestep PEP-668 externally-managed pip.
      - name: Install Pillow (icon-pixel screencap reader)
        run: sudo apt-get update && sudo apt-get install -y python3-pil

      # Boots the emulator, then runs the gate. emulator-up.sh sees the connected
      # device and short-circuits its own boot. Tee combined output to e2e-run.log
      # (run.sh deletes its mktemp watch log in its trap; on timeouts it tails that
      # log to stdout, so the tee captures it).
      - name: Run e2e gate
        uses: reactivecircus/android-emulator-runner@c9c93e6ba9c4194322e114c40970d5983d9a07cc # v2.38.0
        with:
          api-level: 36
          arch: x86_64
          target: google_apis
          force-avd-creation: true
          emulator-options: -no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -camera-back none
          disable-animations: true
          # Don't pipe into tee: a pipeline's exit code is tee's (0), which would
          # mask an e2e failure and keep the job green. Redirect, capture the code,
          # echo the log to the console, then exit with the real code.
          script: bash e2e/run.sh > e2e-run.log 2>&1; code=$?; cat e2e-run.log; exit $code

      - name: Upload e2e log
        if: failure()
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        with:
          name: e2e-run-log
          path: e2e-run.log
          if-no-files-found: warn

      # Mirror run.sh's leaked-watcher kill: the CLI JVM is spawned by the Gradle
      # daemon, not our shell, so killing the wrapper alone leaks it.
      - name: Kill leaked CLI watchers
        if: always()
        run: pkill -f 'dev.hotreload.cli.MainKt' || true

  # Keep cross-version target-project support separate from the configured sample gate
  # above. This suite runs entirely against temporary copies of the fixtures and checks
  # that zero-touch never changes target project inputs.
  compatibility:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # The AGP 8 fixture must run on its target JDK while the CLI remains on JDK 21.
      - name: Install Temurin 17 for the AGP 8 fixture
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '17'

      - name: Export explicit AGP 8 target JDK
        run: |
          test -x "$JAVA_HOME/bin/java" || { echo "JDK 17 installation did not set JAVA_HOME" >&2; exit 1; }
          "$JAVA_HOME/bin/java" -version
          echo "JAVA17_HOME=$JAVA_HOME" >> "$GITHUB_ENV"

      - name: Install Temurin 21 for the CLI
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '21'

      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3.2.2
      - name: Install pinned SDK components
        run: |
          sdkmanager --install \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "ndk;28.2.13676358" \
            "cmake;3.22.1"

      - uses: gradle/actions/setup-gradle@da187c8e6ffbd3802e00f2477aa5a822b25f2dda # v4.4.4

      - name: Install Pillow (icon-pixel screencap reader)
        run: sudo apt-get update && sudo apt-get install -y python3-pil

      - name: Check compatibility fixture and documentation contract
        run: bash e2e/check-compatibility-contract.sh

      - name: Build CLI distribution with JDK 21
        run: ./gradlew :cli:installDist :cli:verifyZeroTouchDistribution

      # The compatibility assertion itself is offline. Prime the two committed fixture
      # dependency graphs first so a cold GitHub-hosted runner does not mistake cache
      # absence for a zero-touch failure.
      - name: Prime compatibility fixture dependencies
        run: JAVA17_HOME="$JAVA17_HOME" bash e2e/run-zero-touch.sh --host-only

      - name: Run offline zero-touch compatibility gate
        uses: reactivecircus/android-emulator-runner@c9c93e6ba9c4194322e114c40970d5983d9a07cc # v2.38.0
        with:
          api-level: 36
          arch: x86_64
          target: google_apis
          force-avd-creation: true
          emulator-options: -no-window -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -camera-back none
          disable-animations: true
          script: |
            test -n "${JAVA17_HOME:-}" || { echo "ERROR: JAVA17_HOME is required; refusing to skip the AGP 8/JDK 17 leg" >&2; exit 1; }
            test -x "$JAVA17_HOME/bin/java" || { echo "ERROR: JAVA17_HOME has no java executable: $JAVA17_HOME" >&2; exit 1; }
            "$JAVA17_HOME/bin/java" -version
            set +e
            ZERO_TOUCH_OFFLINE=1 JAVA17_HOME="$JAVA17_HOME" bash e2e/run-zero-touch.sh > zero-touch-compatibility.log 2>&1
            code=$?
            set -e
            cat zero-touch-compatibility.log
            if grep -Fq 'SKIP: AGP 8/JDK 17' zero-touch-compatibility.log; then
              echo 'ERROR: AGP 8/JDK 17 leg was skipped' >&2
              exit 1
            fi
            grep -Fq 'PASS: AGP 8 zero-touch JDK 17 multi-module hot swap' zero-touch-compatibility.log \
              || { echo 'ERROR: AGP 8/JDK 17 leg did not reach its device assertion' >&2; exit 1; }
            exit "$code"

      - name: Upload zero-touch compatibility log
        if: failure()
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        with:
          name: zero-touch-compatibility-log
          path: zero-touch-compatibility.log
          if-no-files-found: warn

      - name: Kill leaked CLI watchers
        if: always()
        run: pkill -f 'dev.hotreload.cli.MainKt' || true
